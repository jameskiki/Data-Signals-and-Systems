ToDo

Preparation
- verify live UI behavior for overview state restore
- simplify plotting / UI logic where useful
- improve demo signal coverage
- defer for later: add a LaTeX-based technical manual or deeper documentation alongside a simple Markdown README
- defer for later: remove or reduce per-dataset preview state persistence in the main window if it still feels heavy
- defer for later: simplify analysis workspace frequency tools by demoting saved results and advanced methods behind a lighter workflow
- defer for later: move overview plot controls into their own tab or collapsible section while keeping the plot visible
- defer for later: tabulate or further compact the output and channel controls in the preparation UI

Documentation
- add actual screenshots for the main workflow and analysis workspace
- defer for later: add a first-task walkthrough for non-expert users using one concrete example workflow
- validate the user guide wording with a non-expert user and tighten unclear steps
- expand Mermaid diagrams where they improve orientation without cluttering the docs

plotting:
- yy-axis for signals (groupe by oder of magnitude like)

Analysis Workspace
- finish FFT workflow cleanup
- add plot / subplot settings dialog
- unify plot rendering/styling so line widths, font sizes, and line styles can be controlled globally
- support plotting into an existing figure or subplot

Cycle Analysis
- add a dedicated Cycles tab in the analysis workspace
- detect or define cycles and select which to analyze
- compare cycle-to-cycle metrics and trends
- show representative cycle, spread, and drift over time

System Analysis
- add transfer function / frequency response workflow
- add coherence and input-output analysis
- add system identification and model comparison

Model Builder
- build datasets for modeling
- focus on nonlinear models


Questions
- why is data_ops the only folder you ever created? why no other structuring?
- answer: data_ops was the first clean extraction because it holds mostly pure reusable data/signal logic with low UI coupling, so it was the safest part to modularize early.
- answer: the rest of the codebase was already being split by concern without many new folders yet: evaldata_* modules now separate main-window responsibilities, and analysis_workspace_* modules separate analysis layout/state/refresh/views/actions.
- answer: in other words, the project did get more structure, just mostly through top-level module splits first; creating more folders for UI/orchestration layers was deferred to avoid a bigger churn while features were still moving quickly.